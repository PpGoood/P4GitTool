# P4Git Tool 使用分享

---

## 第一章 我为什么要做这个工具

由于vibe coding的流行，agent修改代码是再正常不过的事情，但是由于agent改的代码太多太杂，每个工具的codereview不能包含齐全，所以有一个可视化的review界面就显得十分有意义。

为什么不使用p4的diff原因如下

选择git的原因是agent对git的支持天生就比p4要好得多，虽然agent装mcp也能够去使用p4的功能，但是本质上他的界面就是很臃肿十分难用。

第二是如果有了本地分支就能做快照快速diff，比p4的shver好用而且方便

之前的项目用了git发现分支更难管理，所以这次采用的方式很简单直接本地使用git，提交使用p4这样充分利用git的灵活性但是由于兼容p4以及让软件上手更加简单我舍弃了git的分支概念尽量让agent配合软件一路yes到底







---

## 第二章 怎么用

第一步

![image-20260529165406143](C:\Users\peiyu.li\AppData\Roaming\Typora\typora-user-images\image-20260529165406143.png)

配置p4的设置和对应的git工作区，以及本地的p4工作区



第二步点击init等待配置完成



第三步开始使用

![image-20260529165713109](C:\Users\peiyu.li\AppData\Roaming\Typora\typora-user-images\image-20260529165713109.png)

1提交快照的功能是一次性把本地内容全部提交，暂时没有考虑分批提交的功能，因为提交都可以让agent来做 主要自己只负责代码的codereview

![image-20260529165602894](C:\Users\peiyu.li\AppData\Roaming\Typora\typora-user-images\image-20260529165602894.png)



---

## 第三章 踩过的坑 & 怎么解决

这些都是我自己用下来真实遇到的问题,顺便说下怎么处理。

### 坑1:reconcile 一下冒出来几千个我根本没动过的文件

**为啥会这样**:文件被 Git 动过(内容没变但时间戳变了),P4 一看时间戳不对就以为你改了。或者文件内容确实跟服务器不一样了。

**咋办**:
1. P4V 里 **Get Latest Revision (Force)** 强制同步一遍
2. 回工具点 **"对齐 Git"**

搞定。

### 坑2:点 P4 Sync 只更新了一个文件,结果强制更新发现一大堆没拉到

**为啥会这样**:P4 有个"have 记录"记着你本地有哪些版本,普通 sync 一看记录以为你已经最新了,就跳过了,但其实你本地是旧的。

**咋办**:现在工具的 P4 Sync 默认用强制同步,不看那个记录了,这问题已经修了。万一还碰到,P4V 里手动强制同步一次就好。

### 坑3:reconcile 的时候有文件冲突

**为啥会这样**:你改了某个文件,服务器上别人也改了同一个。

**咋办**:
1. P4V 里 **Resolve** 解决一下(选保留谁的,或者手动合)
2. 回工具点 **"对齐 Git"**

如果对齐的时候工具发现有冲突,会弹个窗让你选:
- **以 P4 版本覆盖本地**:不要我的了,用服务器的(覆盖完会告诉你覆盖了哪些文件)
- **在 Git 中手动解决**:我自己去 Fork / VSCode 解决,弄完再回来点继续



---

## 第四章 这些操作背后到底干了啥

> 这章给想搞明白原理的同学看,不感兴趣的可以跳过。
>
> 核心就一句话:**工具尽量用 Git 的底层命令干活,不切分支、不改磁盘上的文件**,所以不会把 P4 的 reconcile 搞乱。

### 初始化的时候

- 在 Git 仓库目录跑 `git init`,默认分支叫 `mirror/p4`
- 生成 `.gitignore`(忽略 UE 编译产物、那些自动生成的提示文件、`.claude/` 之类)和 `.gitattributes`
- 用 **junction(目录联接,类似快捷方式)** 把 P4 工作区的 `Source`、`Content/Script` 这些目录链到 Git 仓库目录下

这样 Git 仓库和 P4 工作区其实指向**同一份文件**,改一边两边都看得到。

### 提交快照

最简单,就是标准的:
```
git add -A
git commit -m "你写的描述"
```
在 dev 分支上生成一个普通提交,就是时间线上那个黄点。

### 给磁盘"拍照"存进 mirror/p4(最核心的一步)

P4 Sync 和对齐都靠这一步。它牛的地方在于:**给磁盘文件拍张照存进 mirror/p4,但不切分支、不动磁盘。**

它是这么干的:
1. `git read-tree mirror/p4`:把 mirror/p4 的内容读进暂存区
2. `git add`:把磁盘现在的文件加进暂存区
3. `git write-tree`:把暂存区打包成一个 tree
4. `git commit-tree`:基于这个 tree 造一个提交,接在 mirror/p4 后面
5. `git update-ref`:把 mirror/p4 这个指针挪到新提交
6. 最后把暂存区恢复到 dev,刷新一下文件时间戳缓存

整个过程就像"给磁盘拍了张照片存进相册",磁盘本身一个字节都没改。

### P4 Sync(拉代码)背后

1. `p4 sync -f`:强制从服务器下载(不看 have 记录,保证可靠)
2. 把刚下载的最新状态拍照存进 mirror/p4
3. 把 mirror/p4 合并进 dev:
   - 先在内存里试着合,不动磁盘
   - 没冲突 → 直接造个合并提交,挪一下 dev 指针
   - 有冲突 → 老老实实跑真正的 `git merge`,把冲突标记写到文件里让你手动解决
4. 刷新时间戳缓存,免得 reconcile 误报

### 对齐 Git 背后

这个用在"你已经在 P4V 里把磁盘弄成想要的样子,只要 Git 跟上"的情况。**它不合并,直接以磁盘为准。**

1. 把磁盘现状拍照存进 mirror/p4
2. 造一个新提交,**内容直接用 mirror/p4 的**(就是磁盘现状)
3. 把 dev 指向这个新提交
4. 同步暂存区、刷新时间戳

结果:dev 的内容 = 磁盘内容,`git status` 干净;磁盘没动过,P4 reconcile 也干净。

> 划重点:对齐**不碰 P4 的 have 记录**,所以哪怕对齐的时候别人刚提交了新东西,也不会被错误地塞进你的改动列表。(这个之前是个坑,后来去掉了那一步)

### 撤销单个文件背后

```
git checkout mirror/p4 -- 文件路径    # 把文件退回 P4 版本
p4 sync -k 文件路径                   # 告诉 P4 "这文件我是这个版本",免得误报
```

> 老版本这里是对整个工作区做 `sync -k`(几万个文件),慢得要命。现在只针对单个文件,从几秒变成几十毫秒。

### 为啥这套设计能保证 reconcile 干净

P4 判断一个文件"改没改",主要看磁盘文件的内容和时间戳。而工具的核心操作:
- **快照、对齐**:全程用 Git 底层命令操作,**不切分支、不改磁盘文件**
- **P4 Sync**:磁盘文件是 `p4 sync -f` 拉下来的,本来就跟服务器一样
- 每个操作结尾都会刷新一下 Git 对文件时间戳的缓存,避免"内容一样但时间戳不同"被误判

所以正常用下来,工具搞完之后磁盘文件跟 P4 服务器是一致的,reconcile 干干净净。

---

*文档对应 P4Git Tool v1.0*
